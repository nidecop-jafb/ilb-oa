/* coleta-config.js — ILB
   URL vazia = MODO OFFLINE: as paginas funcionam e exportam JSON localmente, sem enviar nada.
   Quando a planilha ILB-Respostas-2026 e o Apps Script existirem (alvo PRE-minhas-entregas),
   preencher COLETA_URL com a URL /exec e republicar. O contrato e o mesmo da FC1 e da FE1. */
/* contrato canonico do ecossistema: o campo e `url` (igual em FC1 e FE1) */
window.COLETA = { url: "" };
